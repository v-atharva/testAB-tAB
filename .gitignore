# data: raw archive files are downloaded via `make data`, never committed.
# Precomputed batch RESULTS are committed (small) so the dashboard works
# straight after clone without downloading the archive.
data/raw/
data/processed/
!data/sample/*
!outputs/results/*

# python
__pycache__/
*.pyc
.venv/
.mypy_cache/
.ruff_cache/
.pytest_cache/
.coverage
dist/

# os / editor
.DS_Store
.idea/
.vscode/
